# Скопируй в .env.production перед сборкой Tauri или прод-фронта.
# Указывает куда клиент должен стучаться за API + WebSocket.

# Прод API-домен (когда поднимем на Selectel):
# VITE_API_URL=https://egvoice.ru

# Временный демо-backend (LiveKit Cloud + туннель):
VITE_API_URL=https://api.egvoice.demo
